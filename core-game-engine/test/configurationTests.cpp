#include "gtest/gtest.h"
#include "gmock/gmock.h"
#include "gtest/gtest.h"
#include "gmock/gmock.h"

#include "treeParser.h"
#include "configuration.h"


TEST(Configuration, POC_1) {
    EXPECT_TRUE(0 == 0);
}

TEST(Configuration, POC_2) {
    EXPECT_FALSE(0 == 1);
}
