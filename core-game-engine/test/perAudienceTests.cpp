#include "gtest/gtest.h"
#include "gmock/gmock.h"

#include "treeParser.h"
#include "perAudience.h"


TEST(PerAudience, POC_1) {
    EXPECT_TRUE(0 == 0);
}

TEST(PerAudience, POC_2) {
    EXPECT_FALSE(0 == 1);
}