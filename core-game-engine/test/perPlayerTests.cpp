#include "gtest/gtest.h"
#include "gmock/gmock.h"

#include "treeParser.h"
#include "perPlayer.h"


TEST(PerPlayer, POC_1) {
    EXPECT_TRUE(0 == 0);
}

TEST(PerPlayer, POC_2) {
    EXPECT_FALSE(0 == 1);
}