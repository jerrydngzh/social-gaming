#include "gtest/gtest.h"
#include "gmock/gmock.h"

#include "treeParser.h"
#include "constants.h"


TEST(Constants, POC_1) {
    EXPECT_TRUE(0 == 0);
}

TEST(Constants, POC_2) {
    EXPECT_FALSE(0 == 1);
}