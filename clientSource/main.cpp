#include <iostream>
#include <memory>
#include "model/include/game.h"
#include "model/include/gameroom.h"
#include "model/include/user.h"

/* TODO:
    1. Main entry point UI:
        a. join game (1 flow)
            - generates an individual 
            - input a game code (random string for now)
        b. create game (1 flow)
            - upload a file
            - generates a "main" display for the game
            - generates join code
*/

int main() {

    // User user;    
    std::cout << "Hello World\n"; 
}

// Andy - we are gitignoring client-build right? 

